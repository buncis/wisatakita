<%= form_for(@wisatum) do |f| %>
  <% if @wisatum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wisatum.errors.count, "error") %> prohibited this wisatum from being saved:</h2>

      <ul>
      <% @wisatum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nama %><br>
    <%= f.text_field :nama %>
  </div>
  <div class="field">
    <%= f.label :harga %><br>
    <%= f.number_field :harga %>
  </div>
  <div class="field">
    <%= f.label :info %><br>
    <%= f.text_area :info %>
  </div>
  <div class="field">
    <%= f.label :foto %><br>
    <%= f.text_field :foto %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
